<UserControl x:Class="PassManaAlpha.MVVM.View.PasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PassManaAlpha.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#943566">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Input Fields -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
            <RowDefinition Height="*"/>
            <!-- ListView -->
        </Grid.RowDefinitions>

        <!-- Input Fields -->
        <Border Background="#763d7a" BorderThickness="2" CornerRadius="8" Margin="284,10,285,76"/>
        <StackPanel Grid.Row="0" Margin="295,15,297,85">
            <TextBox Text="{Binding InputTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="33" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
            <TextBox Text="{Binding InputUsername, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="33" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
            <TextBox Text="{Binding InputPassword, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="33" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </StackPanel>

        <Border Margin="285,143,285,85"  BorderThickness="2" CornerRadius="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,4">
                <Button Style="{StaticResource LocalTypical}" Content="Save" Command="{Binding SaveCommand}" Width="97" Height="30" Margin="0,0,5,0"/>
                <Button Style="{StaticResource LocalTypical}" Content="Load" Command="{Binding LoadCommand}" IsEnabled="{Binding IsLoadEnabled}" Width="89" Height="30" Margin="5,0,0,0"/>
            </StackPanel>
            
        </Border>

        <!-- Buttons -->

        <Border Margin="520,0,0,0" Grid.RowSpan="3" CornerRadius="19">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF763D7A"/>
                    <GradientStop Color="#FF943566" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <ListView Grid.Row="2" ItemsSource="{Binding Entries}" Margin="20">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2D2F38" Padding="10" Margin="5" CornerRadius="5">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding Username}" Foreground="LightGray"/>
                                <TextBlock Text="{Binding Password}" Foreground="LightGray"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
        
        <Border Margin="0,0,510,10" Grid.RowSpan="3">
            
            <TextBox Text="{Binding ConsoleLog}" 
         IsReadOnly="True" 
         VerticalScrollBarVisibility="Auto"
         TextWrapping="Wrap"
         FontFamily="Consolas"
         FontSize="12"
         Background="#111"
         Foreground="#DDD"
         Margin="10,35,10,10"/>


        </Border>
        <StackPanel Margin="10,10,520,230">
            <Button Style="{StaticResource LocalTypical}" BorderThickness="1" Background="Transparent" Content="Clear Console" Command="{Binding ClearConsoleCommand}" />

        </StackPanel>
        
        <Border Margin="305,194,417,43">
             <Button Style="{StaticResource LocalTypical}" Content="Reload Vault" Command="{Binding ReloadCommand}" FontSize="13"/>

        </Border>

       
        

    </Grid>


</UserControl>
